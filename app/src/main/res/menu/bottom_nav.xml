<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_bottom_nav_pin"
        android:enabled="true"
        android:icon="@drawable/ic_content_pin"
        android:title="@string/home_pins"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_bottom_nav_home"
        android:enabled="true"
        android:icon="@drawable/ic_content_home"
        android:title="@string/home_text"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_bottom_nav_history"
        android:enabled="true"
        android:icon="@drawable/ic_content_history"
        android:title="@string/history_header"
        app:showAsAction="ifRoom" />
</menu>